apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dsb-kong-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "dsb-kong-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dsb-kong-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
